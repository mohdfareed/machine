// Plugins — extensions and memory
{
  "enabled": true,

  // Keep ONE memory provider active.
  "slots": { "memory": "openclaw-mem0" },

  "entries": {
    "telegram": { "enabled": true },
    "device-pair": { "enabled": true },
    "llm-task": { "enabled": true },
    "lobster": { "enabled": true },

    // Memory slot companions
    "memory-core": { "enabled": false },
    "memory-lancedb": { "enabled": false },
    "openclaw-mem0": {
      "enabled": true,
      "config": {
        "mode": "open-source",
        "userId": "${MEM0_USER_ID}",
        "oss": {
          // Avoid native sqlite3 bindings in this container/runtime.
          "disableHistory": true
        }
      }
    },

    "voice-call": {
      "enabled": true,
      "config": {
        "enabled": true,
        "streaming": { "enabled": true },
        "tts": { "enabled": true, "mode": "all" }
      }
    }
  }
}
