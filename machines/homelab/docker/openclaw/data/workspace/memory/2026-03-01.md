- Mohammed requested a persistent “check everything” routine.
- Routine to run on request: openclaw status; status --all; gateway status; gateway probe; doctor; channels status --probe; logs --follow (brief sample).
- Report format: ✅/⚠️/❌ with one-line cause per failure.
- Mohammed clarified workflow: workspace/projects/ is for my PRs/testing.
- Do not modify anything outside workspace/ and projects/ (e.g. ~/.openclaw/) without explicit approval; instead, provide instructions/diffs for Mohammed to apply.
- Always document key changes/decisions for future sessions.
- Mem0 OSS plugin status: openclaw-mem0 is enabled but recall/capture repeatedly fails due to missing native sqlite bindings (node_sqlite3.node) on node22 linux-arm64. User does NOT want to disable history; plan is to add build toolchain + libsqlite3-dev in the OpenClaw container image and rebuild sqlite3 bindings for the plugin.
- Proposed container fix (in repo for PR): add apt packages python3, make, g++, pkg-config, libsqlite3-dev to projects/machine/machines/homelab/docker/openclaw/Dockerfile (so sqlite3 native module can compile).
- plugins.allow: user wants explicit allowlist. Updated ~/.openclaw/config/plugins.json5 to include an allow list with openclaw-mem0 and core plugins. However, OpenClaw still warns: "loaded without install/load-path provenance"; allowlist may silence "auto-load" warning but provenance warning may persist without install records.
- Boundary clarification: Mohammed wants me to edit ~/.openclaw/config/*.json5 directly, but for changes in workspace/projects/** (e.g., Dockerfile) I should provide diffs/instructions and/or open PRs; he applies/deploys.
- Security/versioning note: identity/device.json may contain a private key; do NOT version identity/ blindly. Confirm and add ignores for any keys.
- Full "check everything" routine persisted in MEMORY.md earlier: status, status --all, gateway status/probe, doctor, channels status --probe, logs --follow (brief sample) with ✅/⚠️/❌ reporting.
