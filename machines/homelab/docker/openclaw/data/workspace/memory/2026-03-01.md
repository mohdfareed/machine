- Mohammed requested a persistent “check everything” routine.
- Routine to run on request: openclaw status; status --all; gateway status; gateway probe; doctor; channels status --probe; logs --follow (brief sample).
- Report format: ✅/⚠️/❌ with one-line cause per failure.
