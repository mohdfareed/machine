# 2026-02-28

- User introduced himself as Mohammed Fareed.
- User wants me to be his assistant/copilot and run things for him.
- User prefers I match his vibe.
- Naming and emoji are intentionally deferred for now.
- Mohammed set my first responsibility: manage his homelab, including OpenClaw itself.
- Homelab repo context provided: github.com/mohdfareed/machine
- Mohammed decided my name: Claudia.
- Mohammed prefers conversational pacing (small chunks, back-and-forth).
- External-impact actions need explicit approval; workspace-internal work can be autonomous.
- Asked for proactive manager-style updates/questions/proposals over time.
- Emoji set to 🦉.
- Mohammed wants exploration-first planning and iterative, chunked progress updates.
- Treat container as my workstation; start with onboarding the `machine` repo.
- Ask Mohammed when an action is faster for him than for me (time/effort ratio awareness).
- Agreed workspace model: use `workspace/projects` for active clones/sandboxes; keep root workspace curated.
- Added workspace `.gitignore` + `WORKSPACE-CONVENTIONS.md` to enforce clean persistence boundaries.
- Mohammed wants explicit start/finish signals and shorter, more natural conversational updates.
- Phase 1 approved/executed: credentials perms fixed to 700, hooks verified ready, and daily auth-monitor cron added (08:00 America/New_York).
- Mohammed emphasized collaboration style updates to persist across sessions:
  - Keep replies short, human, conversational.
  - Default to dialogue and iterative one-step flow; avoid monologue/report tone unless asked.
  - Provide proactive visibility during the day: started / done / blocked.
  - Avoid passive stalemates; take initiative and communicate progress.
  - Research best OpenClaw operating patterns (heartbeat vs cron vs hooks/background sessions) before large design changes.
  - Expand assistant scope beyond email/notes and basic coding help toward broader life/ops delegation.
- Repo location pinned: /home/node/.openclaw/workspace/projects/machine
- OpenClaw container Dockerfile: /home/node/.openclaw/workspace/projects/machine/machines/homelab/docker/openclaw/Dockerfile
- Memory-lancedb fix requires @lancedb/lancedb to be present in /app node_modules at build time (not only runtime).
- Switched memory backend to Mem0:
  - Installed plugin: openclaw plugins install @mem0/openclaw-mem0 (v0.1.2)
  - Installer updated /home/node/.openclaw/openclaw.json and switched exclusive memory slot from memory-lancedb -> openclaw-mem0.
  - Gateway restart required after install.
- Installed GitHub skill via ClawHub CLI:
  - clawhub search "steipete github"
  - clawhub inspect openclaw-github-assistant (reviewed SKILL.md and api.js)
  - clawhub install openclaw-github-assistant --force (VirusTotal flagged suspicious; installed after review)
  - Enabled skill in /home/node/.openclaw/config/skills.json5
- Config hygiene fix:
  - Restored /home/node/.openclaw/openclaw.json to include $include-based layout (copied from openclaw.json.bak).
  - Moved Mem0 changes into /home/node/.openclaw/config/plugins.json5 (memory slot -> openclaw-mem0).
  - Updated workspace .gitignore to ignore skills/ and tmp/ directories.
- Rule: avoid letting installers overwrite the whole openclaw.json; prefer applying diffs to config/*.json5 + keep openclaw.json as include-only hub.
- Mem0 plugin issue: mem0 OSS SDK attempts to use sqlite3 native bindings by default (breaks on node22/linux-arm64 without build toolchain).
- Fix applied locally: patched ~/.openclaw/extensions/openclaw-mem0/index.ts to support oss.disableHistory and set config.disableHistory=true when enabled.
- Updated ~/.openclaw/config/plugins.json5 to set openclaw-mem0.config.oss.disableHistory=true and userId='mohammed'.
- Note: gateway restart required for plugin code/config changes to take effect.
