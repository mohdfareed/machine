# =============================================================================
# MARK: Environment and Utilities
# =============================================================================

zmodule homebrew
zmodule environment
zmodule input
zmodule exa --if-command eza

# =============================================================================
# MARK: Completion
# =============================================================================

# Collect completion definitions before initializing completion once.
zmodule zsh-users/zsh-completions --fpath src
zmodule ohmyzsh/ohmyzsh --root plugins/docker/completions --fpath .
zmodule completion
# Load dotnet **after** the main completion is initialized.
zmodule ohmyzsh/ohmyzsh --root plugins/dotnet --if-command dotnet

# =============================================================================
# MARK: Suggestions and History
# =============================================================================

# Load key bindings and fuzzy completion from the installed fzf.
zmodule fzf --use mkdir --if-command fzf --cmd 'source <(fzf --zsh)'

# Preserve the upstream order for widgets that wrap one another.
zmodule zsh-users/zsh-syntax-highlighting
zmodule zsh-users/zsh-history-substring-search
zmodule zsh-users/zsh-autosuggestions
